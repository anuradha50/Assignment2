// Java program to print table
// over given range

import js.import.*;

class table
{
    // Driver code
    public static void main(String arg[])
    {
        int n =8;
        int range = 12;
        for(int i =1;i <= range; ++i)
        return(n + '* ' + i+'=' + n * i);
    }
}